app_identifier("com.kndl.Vault23")

# Your Apple ID (used for App Store Connect auth).
# Set APPLE_ID in your environment or fastlane/.env to avoid committing it.
apple_id(ENV["APPLE_ID"] || "your@apple.id")      # ← update this

# Apple Developer team
team_id("PPLU8J5RDW")

# App Store Connect team (run `fastlane spaceship` to look up yours)
# itc_team_id(ENV["ITC_TEAM_ID"])
